[server]
port = 8020
address = "0.0.0.0"
headless = true
# Local dashboard only — avoids CORS/XSRF conflict warning on LAN access
enableXsrfProtection = false

[browser]
gatherUsageStats = false
serverAddress = "localhost"
serverPort = 8020

[theme]
base = "dark"
primaryColor = "#6366f1"
backgroundColor = "#0f172a"
secondaryBackgroundColor = "#1e293b"
textColor = "#f8fafc"
